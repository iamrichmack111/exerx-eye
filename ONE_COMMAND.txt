After downloading this ZIP, run:

cd ~/Downloads && rm -rf exerx-eye-main && unzip -q exerx-eye-v10-flask-playwright-piper-fixed.zip && cd exerx-eye-main && chmod +x publish_github.sh demo/*.sh && ./publish_github.sh

This corrected build:
- publishes only to git@github.com:iamrichmack111/exerx-eye.git
- runs Flask + pytest + Playwright in normal CI
- does NOT trigger the legacy TUI/PyPI workflow on GitHub Releases
- keeps the old Textual TUI optional only
- creates the GitHub release/tag
- dispatches the separate HQ Piper Demo workflow using en_US-ryan-high
