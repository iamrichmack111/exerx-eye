{
  "name": "exerx-eye",
  "version": "10.0.0",
  "private": true,
  "scripts": {
    "test:ui": "playwright test tests/screenshots.spec.cjs --project=chromium",
    "screenshots": "playwright test tests/screenshots.spec.cjs --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
